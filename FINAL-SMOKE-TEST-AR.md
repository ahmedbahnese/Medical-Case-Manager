# تقرير الفحص العملي لنظام BSCH

تاريخ الفحص: 30 أغسطس 2026

تم تشغيل الخادم بقاعدة SQLite تجريبية معزولة في `/tmp/bsch-test.sqlite`، باستخدام بيانات وهمية بالكامل، دون لمس قاعدة بيانات المستخدم الفعلية.

## النتائج

| الاختبار | النتيجة |
|---|---|
| فحص TypeScript لجميع حزم المشروع | ناجح |
| بناء الخادم والواجهة للإنتاج | ناجح |
| فحص صياغة ملفات Electron وpreload | ناجح |
| بدء الخادم على 0.0.0.0 | ناجح |
| تهيئة SQLite وإنشاء الجداول الافتراضية | ناجح |
| الوصول إلى الواجهة من المنفذ 8091 | ناجح |
| منع الوصول قبل تسجيل الدخول | ناجح — HTTP 401 |
| تسجيل دخول المؤسس | ناجح — HTTP 200 |
| تحميل الأقسام | ناجح — HTTP 200 |
| تحميل الإحصائيات | ناجح — HTTP 200 |
| تحميل بلاغات الحوادث | ناجح — HTTP 200 |
| إضافة حالة جديدة | ناجح — HTTP 201 |
| قراءة الحالات والبحث | ناجح |
| إضافة حالة انتظار | ناجح — HTTP 201 |
| تعديل حالة انتظار | ناجح — HTTP 200 |
| نقل حالة الانتظار إلى قسم | ناجح |
| منع تكرار الحالة عند إرسال أمر الدخول مرتين | ناجح بعد الإصلاح |
| منع تكرار سجل التدقيق عند إعادة نفس الأمر | ناجح بعد الإصلاح |
| بناء نسخة Windows 7 x64 | ناجح |
| بناء نسخة Windows 7 ia32 | ناجح |

## الإصلاحات المنفذة أثناء الفحص

تم إصلاح مسار دخول الحالة من قائمة الانتظار بحيث لا ينشئ سجلًا طبيًا ثانيًا إذا تكرر طلب الدخول لنفس الحالة. كما تم منع إنشاء سجل تدقيق مكرر عند إعادة إرسال نفس الحالة.

تم سابقًا تحويل تصدير لوحة التحكم والأقسام وقائمة الانتظار إلى XLSX حقيقي بدل HTML بامتداد XLS. كما تم إضافة حفظ PDF أصلي داخل Electron، وإضافة التقاط صورة ورقة الطوارئ من كاميرا الهاتف في حالات الانتظار.

## ملاحظة البناء

ظهر تحذير حجم JavaScript أكبر من 500KB في Vite، لكنه تحذير أداء وليس فشلًا؛ البناء اكتمل بنجاح. يمكن تحسينه لاحقًا بتقسيم الصفحات إلى تحميل كسول، لكنه لا يمنع التشغيل.

## الملفات الناتجة

- `electron/dist-electron/BSCH-Portable-ia32-1.0.0.exe`
- `electron/dist-electron/BSCH-Portable-x64-1.0.0.exe`

## حدود الفحص

تم اختبار الخادم وواجهات API فعليًا داخل بيئة معزولة. لم يكن متاحًا تشغيل سطح مكتب Windows 7 الحقيقي داخل بيئة الفحص، لذلك يجب تنفيذ اختبار يدوي أخير على جهاز السيرفر: فتح النسخة، تسجيل الدخول، إضافة حالة، حفظ PDF، فتح XLSX، تصوير ورقة الطوارئ من الهاتف، ثم التأكد من بقاء البيانات بعد إغلاق البرنامج وإعادة فتحه.
