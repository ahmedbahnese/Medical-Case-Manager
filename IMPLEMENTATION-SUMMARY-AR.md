# ملخص تنفيذ طلبات نظام إدارة الحالات الطبية

تم تنفيذ التعديلات على نسخة المستودع في `/home/ubuntu/Medical-Case-Manager`.

## التعديلات المنفذة

1. أضيفت أداة `electron/verify-native-sqlite.js` للتحقق من ترويسة PE لملفات SQLite الأصلية: `PE32` لمعمارية `ia32` و`PE32+` لمعمارية `x64`، مع أمر التشغيل `pnpm --dir electron verify-sqlite`.
2. أصبحت حقول الإشراف والمدير الإداري في بلاغ OVR اختيارية وقابلة للكتابة، مع حماية مسار الإدارة من إظهار Internal Server Error غير معالج.
3. أضيفت حماية تمنع تكرار اسم الحالة في الحالات الحالية وقائمة الانتظار، عند الإضافة وعند التعديل، برسالة «يوجد حالة بنفس الاسم».
4. أضيف HFNC إلى إحصائيات التنفس الصناعي.
5. أصبحت معاينة Excel شاشة جدول كاملة قبل الحفظ، مع زر حفظ ملف XLSX حقيقي، والطباعة والإغلاق، مع الحفاظ على التنسيقات العربية والاتجاه من اليمين إلى اليسار.
6. أضيفت معاينة وحذف واستبدال صورة أو PDF ورقة الطوارئ داخل حوار حالة الانتظار، مع دعم التصوير من الهاتف.
7. أصبح حذف الحالات متاحًا في الواجهة والخادم للمؤسس فقط.
8. أضيفت فلاتر سجل الخروج حسب القسم والتصنيف والتاريخ، وإحصائية للتصنيفات، ومددت مدة الاحتفاظ المعروضة إلى ثلاثة أشهر. كما أضيفت إحصائيات الخروج حسب السبب والقسم إلى لوحة الجودة.
9. أضيف حقل اسم المستشفى المحول إليه عند اختيار التحويل الخارجي من قائمة الانتظار، مع حفظه والتحقق من إدخاله.
10. أضيف حقل `transfer_destination` إلى نموذج وقاعدة قائمة الانتظار مع ترحيل SQLite توافقياً.

## التحقق

نجح ما يلي:

- `pnpm typecheck`
- `pnpm build:api`
- `pnpm build:frontend`
- `node --check electron/verify-native-sqlite.js`
- `git diff --check`

ملاحظة: تحقق DLL سيطبع تحذيرًا إذا لم تكن نسخ SQLite الأصلية الخاصة بـ `ia32` و`x64` موجودة محليًا، وسيوقف البناء برسالة واضحة إذا وجد ملفًا بمعمارية خاطئة.
