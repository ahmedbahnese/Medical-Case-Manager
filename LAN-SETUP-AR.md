# تشغيل Medical Case Manager على شبكة داخلية

## البنية

يعمل جهاز Windows 7 كخادم مركزي. بعد تشغيل التطبيق على هذا الجهاز، تدخل جميع أجهزة الكمبيوتر والهواتف من خلال المتصفح باستخدام عنوان IP الخاص بالسيرفر والمنفذ 8080. جميع المستخدمين يتعاملون مع قاعدة SQLite الموجودة على جهاز السيرفر نفسه، ولا يتم إنشاء قواعد منفصلة على أجهزة العملاء.

## خطوات الإعداد على السيرفر

ثبّت `BSCH-LAN-Win7-32bit-Setup.exe` على جهاز Windows 7، ثم شغّل التطبيق واتركه مفتوحًا. من موجه الأوامر على السيرفر نفّذ:

```bat
ipconfig
```

سجّل قيمة `IPv4 Address`، مثل `192.168.1.50`.

افتح ملف `Allow-LAN-Port-8080.bat` بصلاحية **Run as administrator** مرة واحدة لفتح منفذ التطبيق في جدار حماية Windows.

لجعل الخادم يعمل تلقائيًا بعد كل إعادة تشغيل، افتح ملف `Install-LAN-AutoStart.bat` بصلاحية **Run as administrator** مرة واحدة. سيبدأ Windows تشغيل `StartServer.bat` بعد بدء الشبكة. لإلغاء ذلك استخدم `Remove-LAN-AutoStart.bat`.

## فتح التطبيق من الأجهزة الأخرى

من أي جهاز كمبيوتر أو هاتف متصل بنفس شبكة الواي فاي افتح:

```text
http://192.168.1.50:8080
```

استبدل `192.168.1.50` بعنوان IPv4 الفعلي للسيرفر. في إعدادك الحالي استخدم `http://192.168.100.49:8080`. يجب أن يكون السيرفر والأجهزة على نفس الشبكة. إذا تغير العنوان، أعد تشغيل `StartServer.bat` لمعرفة العنوان الجديد؛ والأفضل تثبيت العنوان من إعدادات الراوتر عندما تتوفر بيانات الإدارة.

## التحليل المحلي للصور وPDF

للاستخلاص من الصور، ثبّت Tesseract OCR محليًا على السيرفر مع حزمة اللغة العربية `ara`، ثم اجعل المسار متاحًا باسم `tesseract` أو اضبط المتغير `TESSERACT_CMD` إلى مسار `tesseract.exe`. لملفات PDF النصية، يحتاج السيرفر إلى أداة `pdftotext` محلية، أو يمكن تحويل الصفحات إلى صور واستخدام OCR عليها. التحليل لا يحفظ الحالة تلقائيًا؛ يعرض القيم المقترحة للمستخدم للمراجعة والتعديل ثم الحفظ.

## ملاحظات الأمان والبيانات

يُحفظ ملف قاعدة البيانات المركزي في:

```text
%APPDATA%\BSCH\bsch.sqlite
```

لا تضع ملف SQLite داخل مجلد مشاركة ولا تفتحه مباشرة من الأجهزة؛ الوصول يجب أن يكون من خلال التطبيق. احفظ نسخة احتياطية دورية من ملف قاعدة البيانات، وغيّر كلمة مرور الدخول الافتراضية بعد أول استخدام.

## النسخة المناسبة

لجهاز Windows 7 الظاهر في الصورة استخدم نسخة `ia32` ذات 32-bit. إذا كان الجهاز Windows 7 بنواة 64-bit يمكن استخدام نسخة x64، لكن نسخة ia32 تعمل عادةً على النظامين.
